<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.autmvc"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="16" />

    <!-- Permission - Internet Connect -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Network State Permissions -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" >

        <!-- android:theme="@style/AppTheme" -->
        <!--
        <activity
            android:name="com.autmvc.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        -->
        <activity
            android:name="com.autmvc.LoginView"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/app_id" />

        <activity
            android:name="com.facebook.LoginActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity
            android:name="com.autmvc.Tabs"
            android:label="@string/title_activity_tabs" >
        </activity>
        <!--
             <activity
            android:name="com.autmvc.LoginView"
            android:label="@string/title_login_view" >   
        </activity>
        -->
        <!-- Pestañas -->
        <activity
            android:name="com.autmvc.MySites"
            android:label="@string/title_activity_my_sites" >
        </activity>
        <activity
            android:name="com.autmvc.FriendsList"
            android:label="@string/title_activity_friends_list" >
        </activity>
        <activity
            android:name="com.autmvc.SitesList"
            android:label="@string/title_activity_sites_list" >
        </activity>
        <activity
            android:name="com.autmvc.Map"
            android:label="@string/title_activity_map" >
        </activity>
    </application>

</manifest>